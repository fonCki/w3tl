import React from 'react';
import WhoToFollow from '@components/Board/WhoToFollowBoard';

const Left = () => {
    return (
        <div>
            <WhoToFollow />
        </div>
    );
}

export default Left;