import React from 'react';
import WhoToFollow from '../../../features/boards/WhoToFollowBoard';

/**
 * Represents the Left component.
 *
 * @function
 * @name Left
 * @returns {React.ReactElement} - The rendered React component.
 */
const Left = () => {
    return (
        <div>
            <WhoToFollow />
        </div>
    );
}
export default Left;